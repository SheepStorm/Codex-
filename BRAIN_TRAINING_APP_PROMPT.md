# Brain Training App（可复用提示词模板）

你是**资深前端工程师 + 认知训练产品工程师**。请实现一个可直接打开运行的 **Brain Training App**，用于网页端认知训练小游戏集合。

---

## 0. 目标与产出

请产出：
1. 一个可直接运行的前端应用（优先单文件 `index.html`，也可多文件但无需构建）。
2. 覆盖以下小游戏模块：
   - 舒尔特表（Schulte）
   - Stroop 颜色干扰
   - 序列工作记忆（WM）
   - 加减法 1-back（错位输入）
3. 具备统一入口流程与一致 UI 风格：
   - 首页 → 训练项目选择 → 难度选择 → 对应训练页面
4. 一份简明使用说明（本地运行/部署方式）。

---

## 1. 全局功能要求（必须）

### 1.1 应用结构
- 必须有完整页面流转：
  - `home`
  - `project`
  - `difficulty`
  - `training`
- 每个训练项目必须支持返回上一级。
- 所有状态必须可追踪，避免“卡死在某页”或“按钮失效”。

### 1.2 统一交互规范
- 支持键盘提交（Enter）。
- 输入框必须有合法性约束（仅允许需要的字符）。
- 正确时有轻反馈（绿色短闪）；错误时有红色抖动/提示。
- 提供“暂停/继续”（能暂停计时型训练）。

### 1.3 数据与本地存储
- 每个项目至少记录：
  - 本次成绩
  - 历史最好成绩 `best`
- `best` 使用 `localStorage` 持久化。

### 1.4 代码组织（即使单文件也要分区）
- `config`
- `generator`
- `state`
- `ui/render`
- `handlers`
- `storage`

---

## 2. 游戏模块详细需求

## 2.1 舒尔特表（Schulte）

### 核心玩法
- 网格中按目标序列点击（升序或降序）。
- 点击正确目标才推进到下一个。

### 难度建议
- 基础：4×4（数字）
- 进阶：5×5（数字/字母）
- 高阶：6×6（可扩展古诗词找字）

### 关键要求
- 实时计时。
- 一轮完成后给出本轮耗时。
- 多轮训练时支持轮间休息，并可“点击跳过休息”。

---

## 2.2 Stroop 颜色干扰

### 核心玩法
- 用户应说出文字**颜色**而非字义。
- 可有干扰字与随机目标定位（高级模式）。

### 难度建议
- 基础：固定网格逐项高亮
- 进阶：增加干扰字
- 高级：成组展示 + 箭头定位

### 节奏要求
- 每轮有固定时长或固定条目数。
- 轮间休息统一（建议 10 秒），支持鼠标左键跳过休息。

---

## 2.3 序列工作记忆（WM）

### 核心玩法
- 展示序列后进行复述。
- 支持顺序/逆序。
- 支持数字/字母/emoji（可扩展）。

### 难度策略
- 基础：单阶段（仅复述），随机顺序/逆序。
- 进阶：双阶段（`1.复述` + `2.定位第 n 位`）。
- 高级：预留。

### 关键规则
- 序列生成避免连续重复字符（如 `ee226` 不允许出现 `ee`）。
- 基础模式可配置为纯数字或纯字母序列（单题内不混合）。
- 如启用双阶段，第二阶段只在第一阶段提交后进入。
- “查看答案”建议在至少一次提交后再显示。

---

## 2.4 加减法 1-back（错位输入）

### 核心玩法（必须严格）
- 你永远回答“上一题”的答案，不是当前题。
- 题 1：仅展示，不作答。
- 题 2：作答题 1。
- 题 3：作答题 2，以此类推。

### 计时要求
- 第一题展示时间：`3s`。
- 后续每题展示/输入时间：`4s`。
- 超时按错误处理，立即 Game Over。

### 出题要求
- 10 以内加减法，可配置：
  - 模式 A（默认）：操作数与结果都限制在 `0..10`
  - 模式 B：仅操作数限制 `0..10`，结果可负
- 加减比例默认 `50/50`。

### 必要数据结构（建议）
```ts
Question = {
  id: number,
  a: number,
  b: number,
  op: '+' | '-',
  text: string,
  answer: number
}

AppState = {
  running: boolean,
  paused: boolean,
  currentQ: Question | null,
  pendingQ: Question | null,
  qIndex: number,
  streak: number,
  best: number,
  leftSec: number,
  phase: 'first_show' | 'active' | 'over',
  history: Array<{
    qShown: Question,
    pending: Question | null,
    userInput: string,
    correct: boolean
  }>
}
```

---

## 3. UI/UX 统一要求

- 暗色系、对比清晰，移动端优先。
- 训练页采用“信息区 + 交互区”结构，状态信息可见：
  - 当前题号
  - 当前成绩（streak）
  - 历史最佳（best）
  - 计时（如适用）
- Game Over 面板必须包含：
  - 本次成绩
  - 历史最佳
  - 最近若干条记录（建议 5 条）
  - 再来一局按钮

---

## 4. 稳定性与边界条件

- 禁止无意义 `setInterval` 刷新 UI。
- 定时器必须在页面切换/结束时清理。
- 第 1 题不可误提交（1-back 模式）。
- 不允许出现 pending/current 指针错位。
- 输入非法值必须明确提示，不得 silently fail。

---

## 5. 可扩展性预留

请在代码中预留扩展点并注释说明：
- 音效 `playSfx(type)`
- 新项目接入（如 N-back、反应时、Go/No-Go）
- WM 新题型（图形/空间）
- 算术训练扩展乘除法、2-back、自定义时长

---

## 6. 验收清单（请逐项自检）

- [ ] 首页→项目→难度→训练流程完整可返回
- [ ] 舒尔特可正常完成至少一轮
- [ ] Stroop 可正常完成至少一轮并有休息机制
- [ ] WM 基础/进阶流程正确，阶段逻辑无错位
- [ ] 1-back 算术：第一题 3s，仅展示；后续每题 4s 且答上一题
- [ ] 错误或超时立即结束并显示复盘
- [ ] best 可刷新后保留

---

## 7. 输出格式要求（给实现模型）

请直接输出完整可运行代码；如果是单文件请输出完整 `index.html`，包含：
- HTML 结构
- CSS 样式
- JS 逻辑
并在关键函数处写注释解释状态机与计时逻辑。

